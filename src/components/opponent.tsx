"use client"

interface OpponentProps {
    handleOpponent: () => void;
    opponent: any;
  }
  
  const Opponent: React.FC<OpponentProps> = ({ handleOpponent, opponent }) => {
    // Rest of the logic related to the opponent
  
    return (
      // JSX for the Opponent component
      <>
      
      </>
    );
  };
  
  export default Opponent;
